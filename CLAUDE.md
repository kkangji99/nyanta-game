# 프로젝트 규칙

## 최우선 규칙
**모든 작업은 최적화와 보안 이슈가 생기지 않게 한다.** 기능을 추가·수정할 때마다 아래를 확인하고, 문제가 될 만한 점은 작업 안에서 같이 해결한다.

## 최적화 체크리스트
- 매 프레임 도는 코드(`update`, `render`)에 새 배열·객체·클로저를 만들지 않는다. 배열 정리는 `sweep`을 쓴다.
- 반복해서 그리는 그림은 `sprite()`로 오프스크린 캔버스에 캐시하고 `blit`으로 재사용한다.
- 화면 밖 요소는 그리지 않는다(적, 고등어, 입자, 피해 숫자, 울타리 기둥).
- 같은 색·모양은 `Path2D` 하나로 묶어 `fill` 호출 수를 줄인다.
- 캔버스 픽셀 수(DPR)에 상한을 둔다.
- 목록에는 상한을 둔다(적 `MAX_E`, 고등어, 입자, 피해 숫자).
- 변경 후에는 실제 브라우저에서 후반부 상황(적 ~80마리)의 `render` 소요 시간을 재서 60fps 예산(16.7ms) 안에 드는지 확인한다.

## 보안 체크리스트
- 비밀 키는 저장소에 넣지 않는다. Supabase는 브라우저 공개용 publishable 키만 사용하고, secret/service_role 키는 절대 쓰지 않는다.
- 사용자 입력(닉네임 등)을 `innerHTML`에 넣을 때는 반드시 `esc()`로 이스케이프한다. 가능하면 `textContent`를 쓴다.
- `eval`, `new Function`, 신뢰할 수 없는 외부 스크립트를 쓰지 않는다.
- DB는 행 단위 보안(RLS)으로 조회·등록만 허용하고 수정·삭제는 막는다. 값의 범위 검사와 연속 등록 제한은 DB 쪽에도 건다.
- 클라이언트가 보내는 점수는 신뢰하지 않는다. 말이 안 되는 값은 DB 제약으로 거른다.
- `index.html`의 CSP(Content-Security-Policy) 메타 태그를 유지한다. 외부 접속처를 늘릴 때만 목록에 추가한다.

## 작업 방식
- 코드를 고치면 `node --check`로 문법을 확인하고, 로컬 서버 또는 헤드리스 Chrome으로 실제 동작을 확인한 뒤 커밋한다.
- CSS/JS를 바꿔 배포할 때는 `index.html`의 `?v=` 값을 올려 캐시를 무효화한다.
- 한국어로 커밋 메시지와 주석을 쓴다.
